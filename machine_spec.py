# This is the basic file that will generate information for the creation of the VM.
sysname = input('Enter the machine name:')
type = input('Enter the type of system')
location = input('Enter the location')
